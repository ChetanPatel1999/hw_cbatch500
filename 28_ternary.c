//wap to check two given number are same or different.
#include <stdio.h>
void main()
{
    int num1, num2;
    printf("enter first num :");
    scanf("%d", &num1); 
    printf("enter second num : ");
    scanf("%d", &num2); 
    num1 == num2 ? printf("number are same") : printf("num are different");
}